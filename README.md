# 명령어
- uv run uvicorn app.main:app --reload                     [실행]
- uv run ruff check .                                      [모든 파일 러프 체크]
- uv run ruff check . --fix                                [자동 수정]
- uv run ruff format .                                     [코드 줄 맞춤]
